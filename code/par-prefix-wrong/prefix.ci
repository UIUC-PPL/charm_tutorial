mainmodule prefix{
  readonly CProxy_Main mainProxy;
  readonly int numElements;
  readonly CProxy_Prefix prefixArray;

  mainchare Main {
    entry Main(CkArgMsg*);
    entry void done();
  };

  array [1D] Prefix {
    entry Prefix();
    entry void run();
    entry void getValue(int, int);
  };
};
