mainmodule pi {
 
  mainchare Master {
    entry Master(CkArgMsg *m);
    entry void addContribution(int numIn, int numTrials);
  };
  
  chare Worker {
    entry Worker(int numTrials, CProxy_Master main);
  };

};
